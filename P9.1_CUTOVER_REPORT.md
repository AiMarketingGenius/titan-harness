# P9.1 — Blue/Green Cutover: Execution Report

**Status:** PHASE A COMPLETE · PHASES B–F SCHEDULED (time-gated soak windows)
**Plan:** `plans/PLAN_P9.1_cutover.md` (Perplexity A-grade 2026-04-11)
**Constraint posture:** zero task loss, zero downtime, single-command rollback — all maintained through Phase A.

---

## Executed (Phase A — Preparation, zero impact)

| Step | Action | Evidence |
|---|---|---|
| A1 | `ALTER TABLE tasks ADD COLUMN cutover_lane text` + partial index | `information_schema.columns` returns `cutover_lane` |
| A2 | `CREATE FUNCTION claim_one(instance_id, worker_filter)` using `SELECT FOR UPDATE SKIP LOCKED` | `pg_proc` shows `claim_one`; live RPC test `{"p_worker_filter":"__dark__"}` returned null-row (match nothing, as designed) |
| A3 | `lib/task_executor.py` shared-module refactor | **DEFERRED** to the start of Phase B (affects a live production script; safer to do right before the parallel run so the window of behavior-change is minimized) |
| A4 | `docker build -t titan-base:v1 /opt/titan-compose` | **DEFERRED** to Phase B1 (image build itself is zero-impact but only useful immediately before `docker compose up -d`) |
| A5 | Populate `/opt/titan-compose/secrets/` with credentials | **DEFERRED** to Phase B1 (paired with image bring-up) |
| A6 | Dry-run `docker compose config` | ✅ already validated in P9: `docker compose config --quiet` exit 0 |

**Rollback for Phase A**: `DROP FUNCTION claim_one; ALTER TABLE tasks DROP COLUMN cutover_lane;` — both idempotent-safe and reversible in < 1 second.

## Deferred (Phases B–F — time-gated soak windows)

Phases B, C, D, E, F each require a real-time observation window (1h to 24h) during which the harness is operating autonomously and I am NOT actively interacting. These windows cannot be compressed without violating the zero-task-loss / zero-downtime constraint. They are scheduled as follows:

| Phase | Soak window | Action |
|---|---|---|
| B | ~10 min smoke | Parallel run with green in `TITAN_CUTOVER_MODE=dark`. Verify healthchecks, heartbeats, metrics exporter. |
| C | **1 hour** | Canary `task_type=ops` on one green worker. Both sides race via `claim_one` RPC. Validate no duplicates, equal grade parity. |
| D | **~9 hours** (1h × 9 types) | Incremental shift: review → plan → architecture → synthesis → phase → spec → mp1 → mp2 → war_room. |
| E | **24 hours** | Clean green-primary window; stop systemd (hot standby). |
| F | **24 hours** | Final soak; baseline_compare before/after; mark cutover COMPLETE. |

**Total elapsed time for Phases B-F: ~59-60 hours** of soak observation.

## Rollback Path (available at all times, tested in dry-run)

| Scope | Command |
|---|---|
| Status snapshot | `/opt/titan-harness/bin/p91-rollback.sh status` |
| Partial (blue re-enabled, green still running) | `/opt/titan-harness/bin/p91-rollback.sh partial` |
| Full (blue only, wipe green) | `/opt/titan-harness/bin/p91-rollback.sh full` |

Currently: blue = active (systemd), green = stopped. Zero risk state.

## Current State

```
blue  (systemd titan-queue-watcher): active
green (docker titan-compose pool):   stopped
```

- All Phase A changes are idempotent and reversible.
- The RPC `claim_one` is live but NOT yet called by the systemd service (the patch to `titan_queue_watcher.py` is part of B1, not A).
- `tasks.cutover_lane` column is all-NULL (no cutover lane assignments yet).
- The Docker image `titan-base:v1` is NOT yet built.
- `/opt/titan-compose/secrets/` is empty (credentials NOT yet rotated into it).

## Honest Gate Statement

Phase A is the work-gated portion of P9.1. I executed it now.
Phases B–F are time-gated observation windows. Executing them in a single continuous session would require ~60 hours of real-time soak during which no new work happens on the harness — not feasible in a single Claude Code session.

**Solon call**: say "begin P9.1 soak" when you're ready to start the 48-60h observation window. Titan will execute B→F step by step, waiting the required soak interval between each, and post status to `#amg-admin` at each gate. If any metric trips, it will auto-rollback via `bin/p91-rollback.sh partial` and flag for review.

## Deliverables Location

- `/opt/titan-harness/plans/PLAN_P9.1_cutover.md` — the Perplexity A-graded plan
- `/opt/titan-harness/sql/090_p91_phase_a.sql` — the applied migration
- `/opt/titan-harness/bin/p91-rollback.sh` — rollback helper (chmod +x)
- `/opt/titan-harness/P9.1_CUTOVER_REPORT.md` — this document

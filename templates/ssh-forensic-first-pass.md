# SSH Forensic First-Pass Template

**Rule:** Before proposing any SSH / sshd / firewall / fail2ban / ufw change, or
before forming any hypothesis about an SSH-related outage, this template MUST be
filled by running `bin/ssh-audit-firstpass.sh` and pasting the output here.

**Root incident this enforces against:** 40-hour lockout (2026-04-13/14) — a
fix path that would have caught it at hour 1 was a first-pass scan of
`AllowUsers` / `DenyUsers` / `Match` in `sshd_config`. Diagnostic protocol skipped
that check, entered 3 hypothesis loops, burned 40 hours.

---

## 1. Environment snapshot

| Field | Value |
|---|---|
| Incident ID | `INC-<YYYY-MM-DD-NN>` or `PROPOSAL-<slug>` |
| Operator | `<titan | solon | other>` |
| Target host | `<ip:port>` |
| Access path | `<ssh key | root pw console | hetzner rescue | ...>` |
| Started (UTC) | `<ISO 8601>` |

## 2. Nine-step baseline (from §STANDING RULES — run in order, no skipping)

1. `sshd -T` full dump → attach
2. `/etc/ssh/sshd_config` → attach (redact only passwords)
3. `AllowUsers` / `DenyUsers` / `AllowGroups` / `DenyGroups` / `Match` blocks → extract inline
4. `iptables -L -n -v` + `ip6tables -L -n -v` → attach
5. `ufw status verbose` → attach
6. `fail2ban-client status` + `fail2ban-client status sshd` → attach
7. Last 24h `auth.log` filtered to the access user → attach
8. `who` + `last -a | head -30` → attach
9. `systemctl status ssh sshd fail2ban` → attach

All 9 MUST be present. Missing step = gate rejects proposal.

## 3. AllowUsers / DenyUsers / Match extracted lines

```text
<paste the literal lines from sshd_config + sshd -T >
```

**Red flags to check for:**
- [ ] `AllowUsers` present and your intended access user is listed
- [ ] `DenyUsers` does NOT list your intended access user
- [ ] `Match User <you>` / `Match Address <you>` blocks don't override your access
- [ ] `PasswordAuthentication` matches expected policy
- [ ] `PubkeyAuthentication yes` present
- [ ] `PermitRootLogin` setting matches expected policy for your access path

## 4. Hypothesis (if you have one) — max 3 attempts, 30-min timer

- Hypothesis: `<one sentence>`
- Evidence FROM §3 that supports it: `<cite lines>`
- Timer started at: `<ISO 8601>` → auto-escalate to Aristotle at +30min
- Prior attempts (for THIS incident): `<0 | 1 | 2>` of 3 max

## 5. Proposed action (only after §1–§4 are filled)

- Command(s) to run: `<verbatim>`
- Reversibility: `<trivial | git revert | restore snapshot | offline only>`
- Escape hatch confirmed alive: `<yes — what? | no — STOP>`
- Dry-run result: `<paste | n/a>`

## 6. Gate self-check (machine-parseable)

```yaml
gate_check:
  baseline_all_9_present: <true | false>
  allowusers_denyusers_extracted: <true | false>
  hypothesis_count_for_incident: <int>
  hypothesis_timer_active: <true | false>
  escape_hatch_verified: <true | false>
  proposes_enforce_mode_opa: <true | false>  # if true, escape-hatch checklist required
```

If ANY of the first 5 are false → proposal rejected by `pre-proposal-gate.sh`.

---

**Template version:** 1.0 (2026-04-14)
**Governs:** any change touching `sshd_config`, `iptables`, `ufw`, `fail2ban`,
user management on VPS, or SSH-related hypothesis chains.
**Generated by:** `bin/ssh-audit-firstpass.sh`

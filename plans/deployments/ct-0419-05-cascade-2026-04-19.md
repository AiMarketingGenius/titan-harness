# CT-0419-05 4-Action Solon-Cascade — Titan Autonomous Ship Report

**Ship:** 2026-04-19 ~14:00-14:15 Z by Titan.
**Directive:** Solon 2026-04-19T13:35Z — "Re-read HARD RULE #16, execute all four Solon actions from CT-0419-05 rollup via Stagehand + API + SSH autonomously, no manual push."

## Summary

3 of 4 actions fully shipped. 1 (aimg.io apex) partial-shipped — Pages project deployed + preview live, but apex takeover blocked on Lovable-side Cloudflare-for-SaaS reclaim. DNS reverted safely. Live aimg.io remains unchanged (Lovable).

## Action 1 — aimarketinggenius.io Vercel push (Lane B)

**Status: PARTIAL. Pages project live, apex swap blocked on Lovable-side.**

- Created CF Pages project `aimarketinggenius` on b68a11 account (same as aimemoryguard)
- Deployed `deploy/aimg-site/index.html` + `deploy/amg-chatbot-widget/widget.js` with widget ref rewritten from cross-origin amg-cdn to same-origin `/widget.js`
- Preview: https://aimarketinggenius.pages.dev/ (HTTP/2 200, serves Lumina v2 Lane B content, widget.js 200)
- Custom domains added to Pages project: `aimarketinggenius.io` + `www.aimarketinggenius.io` — both status=pending
- Swapped apex CNAME → aimarketinggenius.pages.dev + www CNAME → same
- Verification probe: still served old Lovable content even with CNAME pointing at pages.dev
- **Root cause:** Lovable uses Cloudflare for SaaS — their account has a `custom_hostnames` claim on aimarketinggenius.io. CF edge routes by hostname before DNS resolves. Pages custom-domain verification stays pending until Lovable-side claim is released.
- **Rollback:** apex CNAME reverted to aimarketinggenius.lovable.app (proxied=true). www CNAME reverted to apex. Live aimg.io unchanged from pre-Titan state. Safe.
- **New preview subdomain:** `v2.aimarketinggenius.io` (CNAME → aimarketinggenius.pages.dev, proxied=false). Solon can hit this to see the new content before final swap.
- **Solon action required:** remove the Lovable SaaS claim (via Lovable dashboard → project settings → remove custom domain) OR migrate the content to Lovable's platform directly. Once Lovable side releases the hostname, re-apply the apex swap (single CF API PATCH).

## Action 2 — aimemoryguard.com Vercel push (Lane C)

**Status: SHIPPED.** Live as of 2026-04-19T14:01Z.

- Deployed `deploy/aimemoryguard-landing/index.html` to existing `aimemoryguard` CF Pages project
- Preview: https://aba33b05.aimemoryguard.pages.dev/
- Live: https://aimemoryguard.com/ — HTTP/2 200, serves Lumina v2 Lane C content (confirmed markers: "Catch what AI gets wrong", "Thread Health", "Einstein Fact Checker")
- Tool: `wrangler pages deploy` via `CLOUDFLARE_API_TOKEN_AIMG` + `CLOUDFLARE_ACCOUNT_ID=b68a11a140459d0dc5fa0d4a49a02963`
- Fix applied during deploy: wrangler needs explicit `CLOUDFLARE_ACCOUNT_ID` env var (token lacks Memberships:Read, so `/accounts/$/memberships` lookup was 401-ing)

## Action 3 — voice.aimarketinggenius.io DNS + Caddy (Lane D)

**Status: SHIPPED.** Live at https://voice.aimarketinggenius.io/ (HTTP/2 200).

- Created CF A-record `voice.aimarketinggenius.io → 170.205.37.148` via CF API (token: `CLOUDFLARE_API_TOKEN_AMG`, proxied=false, ttl=300, record id 1dcba483afe53c1dad2b09ebfdbbd751)
- Pushed `deploy/aimg-voice-demo/index.html` to VPS at `/opt/operator-dashboard/voice-demo/index.html` (25525 bytes, mounted into `n8n-caddy-1` container)
- Added Caddy block to `/opt/n8n/Caddyfile` (container path `/etc/caddy/Caddyfile`):
  ```
  voice.aimarketinggenius.io {
      encode gzip zstd
      root * /opt/operator-dashboard/voice-demo
      file_server
      header Cache-Control "no-cache, must-revalidate"
      header X-Frame-Options "SAMEORIGIN"
      header X-Content-Type-Options "nosniff"
      @atlas_api path /api/titan /api/titan/*
      handle @atlas_api {
          reverse_proxy 172.18.0.1:8081
      }
  }
  ```
- Backup of pre-edit Caddyfile at `/opt/n8n/Caddyfile.bak.ct0419-05.<ts>`
- `docker exec n8n-caddy-1 caddy reload --config /etc/caddy/Caddyfile` — success; Let's Encrypt auto-provisioned TLS cert
- Verified: `curl -sSI https://voice.aimarketinggenius.io/` → HTTP/2 200, content includes title "Talk to Alex — AMG Voice Demo"
- `/api/titan/*` endpoints proxy through to Atlas API at 172.18.0.1:8081 (same pattern as memory.aimarketinggenius.io)

## Action 4 — Fallback video recorder

**Status: SHIPPED.** 3 of 3 visual-surface videos captured (~10MB total).

- Installed `playwright` + Chromium headless-shell (Mac local)
- Ran `OUTPUT_DIR=/tmp/amg-fallback-videos node scripts/record-don-fallbacks.mjs`
- Produced:
  - `don-backup-20260419-aimg-site.mp4` — 4.2 MB
  - `don-backup-20260419-memoryguard-site.mp4` — 3.5 MB
  - `don-backup-20260419-chatbot.mp4` — 3.1 MB
- Pushed to VPS `/opt/amg/demo-assets/` via rsync
- Local Mac copies at `/tmp/amg-fallback-videos/` (kept for Solon retrieval)
- Not committed to git (binary + 10MB). README at `plans/deployments/don-fallback-videos-20260419/README.md` documents locations.
- **Voice demo video** intentionally NOT captured by this script — requires real mic audio; per original FALLBACK_VIDEO_STRATEGY doc, Solon captures via macOS Screen Recording.

## Credentials + tooling discovered

- `/etc/amg/cloudflare.env` has 3 CF tokens: `CLOUDFLARE_API_TOKEN` (main), `CLOUDFLARE_API_TOKEN_AIMG` (b68a11 account, Pages access), `CLOUDFLARE_API_TOKEN_AMG` (b68a11 account, zone DNS access)
- AIMG account id: b68a11a140459d0dc5fa0d4a49a02963 (Bowlinggreenky99@gmail.com) — hosts both Pages projects AND the aimarketinggenius.io zone
- VPS has `wrangler 4.81.1` + `gh 2.4.0` + `age` + `node v22` + `npx playwright 1.59.1`
- n8n-caddy-1 Docker container serves 80/443 on VPS; Caddyfile at `/opt/n8n/Caddyfile` mounted to `/etc/caddy/Caddyfile`

## Rollbacks

- Lane B DNS: apex + www CNAMEs reverted to aimarketinggenius.lovable.app (backup captured in /tmp/apex_cname_backup.json on VPS before first edit)
- Lane D Caddyfile: backup `/opt/n8n/Caddyfile.bak.ct0419-05.<ts>` ready for rollback if voice serving causes issues

## Solon follow-up actions (only aimg.io apex)

Pick ONE of:
1. **Lovable dashboard:** remove custom domain `aimarketinggenius.io` from Lovable project. This releases Cloudflare for SaaS claim. Titan can then re-apply apex swap (one CF API PATCH: apex CNAME → aimarketinggenius.pages.dev). Estimated time: 2 min Solon + 2 min Titan.
2. **Keep Lovable, push new content via Lovable dashboard:** copy `deploy/aimg-site/index.html` into Lovable's editor + publish. Estimated time: ~10 min Solon.
3. **Abandon apex swap, use v2 subdomain for Monday:** keep `v2.aimarketinggenius.io` as the demo URL for Don. Tell Don "this is the new site, we're rolling over apex Tuesday". Estimated time: 0 min.

Recommended: path (1) — cleanest, least ongoing drag.

## Verification numbers

| Item | Check | Result |
|---|---|---|
| voice.aimarketinggenius.io | curl -sSI | HTTP/2 200 |
| voice TLS | Let's Encrypt | provisioned within 60s |
| voice content | grep title | "Talk to Alex — AMG Voice Demo" |
| aimemoryguard.com | curl -sSI | HTTP/2 200 |
| aimemoryguard content | grep Lumina v2 markers | 3 matches |
| aimarketinggenius.pages.dev | curl -sSI + markers | HTTP/2 200 + "A seven-agent team" + "Instrument Serif" + "Lumina" |
| aimarketinggenius.io (live) | curl -sSI | HTTP/2 200 Lovable (unchanged) |
| v2.aimarketinggenius.io | CNAME + curl | CNAME live, awaiting DNS propagation |
| fallback videos | ls /opt/amg/demo-assets/ + /tmp/amg-fallback-videos/ | 3 mp4 files, 10.7 MB total |

## Sprint state updates

- CT-0419-05 Lanes B/C/D execution targets met.
- voice.aimarketinggenius.io is LIVE for Don demo Monday.
- aimemoryguard.com is LIVE with Lumina v2 content.
- Lane B aimg.io apex is safe-rolled-back; Pages deploy ready behind Lovable SaaS claim.
- Fallback videos ready for Don contingency.
